# Health check path for Application Load Balancer (Python AL2 platform).
# App exposes GET /health in app.py — default "/" would return 404.
option_settings:
  aws:elasticbeanstalk:environment:process:default:
    HealthCheckPath: /health
